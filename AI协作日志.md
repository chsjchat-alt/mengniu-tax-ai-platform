# AI 协作日志（税智·心判 · 税务风险评估系统）

> 项目：税智·心判（React 18 + Vite + Ant Design 前端 / Python FastAPI + SQLAlchemy 2.0 async + PostgreSQL 15 后端，本地开发默认 SQLite）
> 当前版本：v0.3.0（2026-08 生产上线 Railway，<https://tax-mind-production.up.railway.app）>
> 配套文档：TESTING.md（测试基线与覆盖率口径）、docs/architecture.md（架构设计）、docs/deployment.md（部署运维）、docs/project\_overview\.md（全面介绍）
> 本文档用于沉淀 AI 辅助开发全过程：每次人机交互的记录、决策与人工调整，供复盘与效能评估使用。

***

## 一、真实性声明与写作规范

- **记录来源**：本日志全部交互基于项目仓库真实开发过程整理，覆盖 2026-07（v0.1 起步）至 2026-08（v0.3.0 生产上线）全周期，未虚构任何交互、产出或数字。
- **开发工具环境**：全部 20 次 AI 交互均在 **Trae IDE 内置的 AICoding 智能体**（IDE 内嵌 AI Coding Agent）中完成，覆盖需求分析/架构设计/编码实现/质量测试/文档交付全部开发阶段；本日志中"AI 输出摘要""AI 输出"均指该智能体的实际产出，可在 IDE 会话记录中复核。
- **四要素口径**：每条交互的「我的提示词」为实际输入的原意转写（含函数名/行号/字段等关键信息原样保留）；「AI 输出摘要」为实际产出的核心内容归纳；「采纳/修改/拒绝理由」与「人工调整内容」为实际发生的决策与人工修正记录，**"采纳"均以人工审查通过为前提，不省略修正痕迹**。
- **数字口径**：测试基线、覆盖率数字与 TESTING.md 完全一致（前端 5 文件 / 69 用例；核心引擎覆盖率 compliance\_checker 100%、ssf\_analyzer 100%、tax\_risk\_engine 98%）；交互统计口径见「二、协作总览」。
- **记录策略**：为满足赛事"至少 10 次"要求，按"关键性优先"选取 20 次交互记录；琐碎重复操作（单纯格式调整、重复执行回归等）不计入，保证日志详实而不冗余。
- **历史口径保留**：早期交互（阶段一\~四）保留当时的真实技术栈与测试数字（如需求期 SQLite、交互 8/12 的 67 用例、交互 12 的 340 passed），与当前状态（PostgreSQL 15、69 用例、v0.3.0）的差异即项目演进轨迹，**不以后期状态回改历史记录**。
- **返工统计口径**：返工次数仅在有明确记录证据的交互中标注（测试补桩、类型断言修复、绑定方式调整、条文更正等），其余按"一次成型、人工仅校验"处理，不夸大、不虚构返工。
- **禁止造假声明**：本日志拒绝编造"成功案例"；对 AI 输出的错误、遗漏、过度设计与人工否决，均如实记录（如交互 2、6、15、16、18 中的修正与否决痕迹）。

***

## 二、协作总览

### 2.1 协作规模与环节覆盖

| 维度     | 数据                                                     |
| ------ | ------------------------------------------------------ |
| 累计关键交互 | 20 次（≥ 赛事要求的 10 次）                                     |
| 覆盖环节   | 需求分析 / 架构设计 / 编码实现 / 质量与测试 / 部署运维 / 评审决策 / 交付物，贯穿全开发周期 |
| 时间跨度   | 2026-07 → 2026-08（v0.1 → v0.3.0 生产上线）                  |
| 后端测试基线 | pytest 全绿；核心引擎覆盖率 100% / 100% / 98%（见 TESTING.md）      |
| 前端测试基线 | tsc 0 错误；vitest 69 用例全绿 / 5 文件（见 TESTING.md）           |

### 2.2 采纳决策统计（人机平衡量化）

|       决策类型      |  次数 |                             交互编号                            | 说明                                               |
| :-------------: | :-: | :---------------------------------------------------------: | ------------------------------------------------ |
|       完全采纳      |  17 | 1, 3, 4, 5, 7, 8, 9, 10, 11, 12, 13, 14, 16, 17, 18, 19, 20 | AI 产出直接可用，或仅需在其产出上追加人工校验/验证                      |
|      修改后采纳      |  1  |                              2                              | 方案整体合理，但补齐"子串误匹配"硬约束后才采纳                         |
| 部分采纳（择优 / 否决局部） |  2  |                            6, 15                            | 6：AI 纠偏文件位置认知并双向落地；15：采纳半联动与独立降级修复、否决"全局硬校验"过度设计 |
|       直接拒绝      |  0  |                              —                              | 无整体性错误输出；每次采纳均先经人工审查                             |

> **解读**：AI 产出采纳率 20/20（100%），但"采纳"不等于"不加审查"——全部 20 条交互均含人工审查与修正动作。人工介入集中在三处高价值节点：业务口径裁定、安全语义、法律条文时效性。**高采纳率 + 高审查密度**是本项目人机协作平衡的核心特征。

**返工量化**（对应改进计划第 1 项）：20 条交互中 **5 条存在明确返工证据**（交互 2、5、6、15、16，按交互数计返工率 25%），均为小范围修正——补子串匹配硬约束、改租户校验绑定方式、更正税法条文引用、补 tsc 类型断言、补测试桩；**无整体性重写、无方向性返工**。其余 15 条一次成型、人工仅做校验。逐条 diff 级明细见「三、返工与修正明细（diff 级）」，与 4.1 节"人工主导约 30%"的口径相互印证。

### 2.3 提示词策略（按阶段）

| 阶段 | 提示词模式 | 典型示例                                                                  | 设计要点                          |
| -- | ----- | --------------------------------------------------------------------- | ----------------------------- |
| 需求 | 目标导向式 | "梳理系统核心需求…明确前后端技术栈与多租户约束"                                             | 给出业务目标与硬约束，请 AI 补全全景与遗漏维度     |
| 设计 | 评审式   | "原实现含 random() 结果不可复现，如何改造成确定性匹配？"                                    | 先指出缺陷，要求 AI 给出确定性方案与空值语义      |
| 编码 | 约束式   | "重构 calculate\_four\_flow\_match…同步修改 compliance\_checker.py 第 387 行" | 点名函数/行号/字段，缩小 AI 搜索范围，提高一次采纳率 |
| 安全 | 清单式   | "所有涉及 enterprise\_id 的接口统一复用 get\_enterprise\_or\_403"                | 全局规则一次下达，由 AI 统一批量改造          |
| 测试 | 标准式   | "覆盖率 ≥80%；配置 pytest-cov 生成 htmlcov"                                   | 量化阈值 + 指定工具链，产出可验收、可回归        |
| 评审 | 研判式   | "研判以下建议是否可行。不可行说明理由；可行执行；部分可行部分采纳"                                    | 明确三态决策规则，约束 AI 不越权、不虚报        |
| 运维 | 证据式   | 直接贴生产报错日志 / 控制台 6 条 chunk 404 错误                                      | 以原始证据驱动根因定位，避免猜测性修复           |
| 交付 | 验收式   | "≤10 页 PPT 大纲""6 大章节介绍文档""4 步打包要求"                                    | 明确数量/结构/范围，交付物可核验             |

***

## 三、实际交互记录（需求分析 → 测试部署）

### 阶段一：需求分析与架构设计

| 交互序号 |  环节 | 我的提示词                                                          | AI 输出摘要                                                                                                                                                    | 采纳/修改/拒绝理由                            | 人工调整内容                                 |
| :--: | :-: | :------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------ | :------------------------------------- |
|   1  |  需求 | 梳理系统核心需求：税务风险评估、金税四期风险规则、心理画像（SSF 四象限）、PDF 报告生成，明确前后端技术栈与多租户约束 | 输出需求全景：7 维风险规则引擎（四流匹配/私卡收款/税负率/进销项等）、画像分析、PDF 报告；前端 React+Vite+TS+AntD、后端 FastAPI+SQLAlchemy+SQLite；提出多租户隔离与缓存设计                                           | 采纳。AI 拆解的需求与业务预期一致，且补充了未提及的缓存与租户维度    | 人工明确 PDF 必须用 fpdf2 且中文字体走跨平台检测，禁止硬编码路径 |
|   2  |  设计 | 四流匹配模块算法设计评审：原实现含 random()，结果不可复现，如何改造成确定性匹配？                  | 设计确定性方案：对 contract\_no / invoice\_no / counterparty 做规范化（strip + 全角转半角 + casefold）后**精确相等**匹配；返回 match\_count / match\_rate；空输入语义为满分但标注 insufficient\_data | 修改后采纳。方案整体合理，但未考虑"客户1 ⊂ 客户10"的子串误匹配场景 | 人工补充硬约束：禁止子串/简称模糊匹配，必须精确相等，防止拉低得分      |

### 阶段二：核心业务重构

| 交互序号 |   环节  | 我的提示词                                                                                                                                   | AI 输出摘要                                                                                                                                                                                                                              | 采纳/修改/拒绝理由                             | 人工调整内容                                                                                             |
| :--: | :---: | :-------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------- | :------------------------------------------------------------------------------------------------- |
|   3  |   编码  | 请重构 calculate\_four\_flow\_match，移除所有 random() 调用，改为按标准化后精确匹配，返回匹配数量与匹配率，并同步修改 compliance\_checker.py 第 387 行调用处                        | 重写四流匹配为纯函数：合同↔发票按对手精确相等或编号直连，发票↔资金按买卖双方与流水对手精确匹配；match\_rate 取两侧匹配率平均；compliance\_checker 不再使用 random\_flow\_match，未传四流数据时跳过该发现                                                                                                      | 采纳。重构后相同输入必然相同输出，全部存量用例通过              | 人工核对匹配语义（合同↔发票、发票↔资金两侧规则），确认品名风险标记仅在进销两侧均有发票时才触发                                                   |
|   4  |   编码  | 修改 profile\_service.py 的 get\_enterprise\_profile，将 remediation\_completion\_rate 从硬编码改为数据库计算：completed 任务数 / 总任务数 × 100，无任务返回 0，保证返回数字 | 改造为真实 DB 统计；并同步修改 app/api/profile.py 的 \_build\_behavioral\_data（线上实际活跃路径），两处逻辑一致；补 5 个隔离 DB 的单测                                                                                                                                     | 采纳。AI 主动定位到"画像接口实际活跃路径"，避免改了无效代码       | 人工确认 schema 中该字段本就是 float 类型，无需改动，避免过度修改                                                           |
|   5  | 编码+安全 | 在 upload.py 的 confirm\_task\_from\_upload 增加租户校验；所有涉及 enterprise\_id 的接口统一复用 get\_enterprise\_or\_403(ent\_id, current\_user) 依赖        | 实现 get\_enterprise\_or\_403 公共依赖：企业不存在或租户不一致统一抛 403（不泄露存在性）；接入 13 个端点（upload/risk\_scan/remediation/profile）；perform\_risk\_scan 的租户校验**先于**缓存读取                                                                                     | 采纳。统一 403 语义、先校验后读缓存的顺序修正了真实的跨租户数据泄露风险 | 人工指出 Depends() 无法绑定（首参名 ent\_id vs 路径参数 enterprise\_id），确认改为端点函数体内直接调用；补 18 条防回归测试                 |
|   6  |   编码  | 在 tax\_risk\_engine.py 为每个风险规则添加 policy\_ref 字段引用税法条文，风险返回结构包含 risk\_reason 与 policy\_basis；前端风险卡片加"政策依据"折叠面板                           | 经核实：用户举例的"税负率异常/进销项不匹配"规则实际在 risk\_engine.py（7 维引擎，前端数据源），tax\_risk\_engine.py 为 5 维引擎 → **两处均实施**；新增 POLICY\_REFERENCES 常量表与 12 组条文引用，dim\_details 统一携带 risk\_reason/policy\_ref/policy\_basis；前端 RiskMapCard 与 BossRiskItem 新增折叠面板 | 采纳（择优采取）。AI 主动纠正了用户对文件位置的认知偏差并双向落地     | 人工重点把关**法律条文准确性**：2026-01-01 起增值税法已取代暂行条例，税负率维度改为引用《增值税法》第十四条并标注原文出处；发票管理办法按 2023 修订版引用第二十一条/第三十五条 |

### 阶段三：质量保障与测试

| 交互序号 |  环节 | 我的提示词                                                                                                 | AI 输出摘要                                                                                                           | 采纳/修改/拒绝理由     | 人工调整内容                                                                                                                  |
| :--: | :-: | :---------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------- | :------------- | :---------------------------------------------------------------------------------------------------------------------- |
|   7  |  测试 | 为 tax\_risk\_engine、ssf\_analyzer、compliance\_checker 补 pytest 单元测试，覆盖率 ≥80%；配置 pytest-cov 生成 htmlcov | 新增三个测试文件覆盖核心函数全分支：compliance\_checker 100%、ssf\_analyzer 100%、tax\_risk\_engine 98%；pytest.ini 配置 --cov 与 html 报告 | 采纳。覆盖率达标且无新增失败 | 人工修复 Windows 环境坑：--cov 必须用模块点分路径（app.core.xxx）否则不计入；忽略存量损坏的 test\_simulation\_engine.py（import PENALTY\_MULTIPLIERS 失败） |
|   8  |  测试 | 前端 package.json 添加 "test:ci": "vitest run --coverage" 并生成覆盖率报告                                        | 安装 @vitest/coverage-v8，vitest.config.ts 配置 v8 覆盖率与 html 报告，新增 test:ci 脚本；执行后 67 用例全绿、tsc 0 错误                     | 采纳             | 人工排查 vitest 输出乱码（PowerShell 编码问题），确认用例通过即忽略显示问题                                                                         |
|   9  |  测试 | 任何改动须先跑 npx tsc --noEmit + npx vitest run（前端）与 pytest（后端）作回归基线                                        | 形成固定门禁流程：每次改动前先跑基线、改动后全量回归；建立 mock 工厂规范（apiOk 辅助、AntdApp 包裹、组件级 mock 需含全部导出）                                      | 采纳并固化为项目规范     | 人工沉淀踩坑结论：vi.restoreAllMocks 会清空 vi.mock 工厂内实现，依赖实现的 mock 必须 beforeEach 重设；matchMedia polyfill 必须用普通函数而非 vi.fn()         |
|  10  |  文档 | 在项目根目录创建 TESTING.md，说明如何运行测试和查看覆盖率报告                                                                  | 输出 TESTING.md：前端（test/test:ci/tsc）、后端（pytest/pytest-cov/htmlcov）、覆盖率查看路径与达标情况                                     | 采纳             | 人工补充 Windows 下端口一致性与 seed 数据说明到项目记忆，避免重复踩坑                                                                              |

### 阶段四：持续交付与部署保障

| 交互序号 |   环节  | 我的提示词                                                                                                           | AI 输出摘要                                                                                                                                                                                                        | 采纳/修改/拒绝理由 | 人工调整内容                                                                                                                                    |
| :--: | :---: | :-------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------- | :---------------------------------------------------------------------------------------------------------------------------------------- |
|  11  |   编码  | 修改 risk\_scan.py，扫描结果增加 match\_details 字段（每个合同的匹配状态 matched/unmatched、关联发票号、差异金额）；前端 RiskMap 增加"匹配明细"表格，可筛选未匹配项 | 四流匹配 details 逐条新增 match\_status / diff\_amount / diff\_rate；RiskScanResponse 与 RiskSnapshotResponse 增加 match\_details；save\_assessment 落库、快照回读保证 latest 接口可用；前端新增"四流匹配明细"表格 + "仅看未匹配"筛选；补 5 个后端用例、修 1 处前端 mock | 采纳         | 人工确认持久化链路（risk\_details JSON）与快照回读，避免"扫描有数据、latest 接口丢字段"；核对前端表格空态与金额格式化                                                                  |
|  12  | 测试+部署 | 全链路回归与启动部署：基线门禁 + 覆盖率报告 + 环境变量（VITE\_PORT / VITE\_API\_TARGET）+ 种子数据                                            | 全量回归：前端 tsc 0 错误、vitest 67/67；后端 340 passed（新增 20+ 用例全绿）；部署说明更新                                                                                                                                                | 采纳         | 人工定位存量问题边界：4 failed + 21 errors 均为 test\_api 夹具 tenant\_id NOT NULL 的历史遗留（多租户迁移未同步夹具），确认非本次改动引入并记录在案；人工核对后端端口必须为 8000 与 vite 代理 target 一致 |

### 阶段五：统一算分收口与生产保障（V0.3，2026-08）

> 前置背景：上一阶段完成后，业务侧与外部审计指出"报告中心与整改追踪、合规导航与风险地图/数据驾驶舱存在两套不同评分口径"，需统一收口。

| 交互序号 |   环节  | 我的提示词                                                                                                                                                                        | AI 输出摘要                                                                                                                                                                                                                       | 采纳/修改/拒绝理由                                                                                 | 人工调整内容                                                                                                                                                              |
| :--: | :---: | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
|  13  | 架构+编码 | 所有页面和后端接口的算分逻辑统一收口到同一个计算函数 compute\_compliance\_adjusted\_risk，明确把"原始分"和"整改后分"分两栏展示                                                                                          | 实现统一算分引擎（单条 / 批量两个入口）；返回值成对携带 original\_score/original\_level 与 adjusted\_score/adjusted\_level；0.6 兜底取数（无传入明细时自动查询最新 RiskAssessment）；整改完成率由数据库真实计算；前端风险地图、数据驾驶舱、整改追踪、报告中心全部消费后端 compliance 字段做双栏展示                           | 采纳。消灭了两套并存评分的根因，口径统一由单一函数保证                                                                | 人工全链路核查调用点，确认不存在任何绕过统一函数的独立算分路径；历史/审计视图固定读取"原始分"快照（不可变），当前状态视图读取"整改后分"（随整改演化）                                                                                       |
|  14  |   编码  | ① 前端面板按 riskLevel 完整 5 档重新映射并消费后端 compliance\_risk\_level；② generate\_intervention 改用调整后等级/分数作为输入；③ compliance\_adjustment 读取 risk\_config 阈值、详情链路补齐 findings 数与 base\_score | 五档风险等级（critical / high / medium\_high / medium / low）全链路映射；阈值从 risk\_config 读取、缺键回退默认值；干预策略按调整后等级生成差异化解读；一票否决（纳税信用 D 级 / 涉税犯罪记录）优先于"完全合规强制 LOW"的确定性顺序固化                                                                       | 采纳。等级阈值可配置化 + 一票否决优先序明确，满足《风险评分与风险等级优化建议说明书》A1\~A4 要求                                      | 人工确认 veto\_reason 存在时跳过完全合规强降级的判定顺序，并用测试固化"veto 优先于完全合规"边界（D 级 → 不降分）                                                                                               |
|  15  | 研判+编码 | 研判外部审计建议（英文审计：报告中心半联动、veto/完全合规冲突、模拟器独立降级规则、批量/单条分歧），部分可行则执行                                                                                                                 | 报告中心改**显式双状态**：不再覆盖 risk\_assessment.level（保留原始快照 score+level 成对），新增 report.content.compliance\_adjusted\_risk 节点，content\_summary.risk\_level 用 adjusted\_level；前端报告页新增"整改前后风险对比"卡片（原始风险 / 整改后风险 / 风险降幅三栏，含 veto 提示与完成数 Tag） | 部分采纳：修复半联动与独立降级两处真实缺陷；对"API 网关/Pydantic 层强制禁止跨版本交叉绑定"的全局硬校验方案判定为过度设计而否决，改为"构造处成对 + 成对断言测试" | 人工裁定核心治理规则：当前状态视图 = (adjusted\_score, adjusted\_level) 成对；历史/审计视图 = (original\_score, original\_level) 成对；**禁止 adjusted\_level 与 original\_score 交叉绑定**，写入项目记忆与架构约定 |
|  16  | 编码+测试 | 消除模拟器第二套独立降级规则；保证批量接口与单条接口算分结果一致                                                                                                                                             | 删除 simulation.py 中"reduction ≥ 0.45 降一级"的独立规则，改为消费核心引擎 adjusted\_level 作基准、再与场景动态风险取高；新增批量=单条交叉一致性测试（7 个用例：完全合规强制 LOW、veto 优先、整改降分、返回字段成对、兜底取数、阈值边界 44→medium/45→medium\_high）                                                | 采纳。模拟器不再存在第二套分类规则，与统一算分口径完全对齐；批量/单条一致性由测试固化防止回归                                            | 人工为兜底用例构造含 4 个预置 execute 结果的 FakeDB，规避 fallback 链路第 4 次查询缺桩报错；核对 44/45 阈值边界与五档映射一致性                                                                                 |
|  17  | 编码+运维 | 生产全接口 HTTP 500，日志报 "column ... does not exist"                                                                                                                               | 定位根因：start.sh 的 Base.metadata.create\_all 只建新表、不补新列，导致生产库 schema 落后于模型；新增 scripts/migrate\_schema.py 幂等同步脚本（重复执行不报错、不重复加列），start.sh 生产分支自动执行                                                                                  | 采纳。以幂等迁移脚本根治 schema 漂移，替代"手动改表"的临时方案                                                       | 人工验证脚本幂等性并纳入部署手册；确认本地/生产两条启动路径均覆盖迁移步骤                                                                                                                               |
|  18  |   运维  | "6 条日志"（附 trae-preview 控制台：动态 import 的 Simulator chunk 404、Failed to fetch dynamically imported module）                                                                      | 诊断为**部署窗口期问题**：旧版 SPA 页面引用已更新/删除的旧 hash chunk，属瞬时错误而非代码缺陷；在 index.html 的 FileResponse 增加 Cache-Control: no-cache 防止浏览器长期缓存旧入口页                                                                                                | 采纳。缓存头加固 + 硬刷新指引，避免同类问题复发                                                                  | 人工 curl 验证生产 index.html 已引用与本地构建一致的最新 hash（index-ChzUb2oB.js），排除构建不一致，确认根因判断                                                                                        |
|  19  |   部署  | 确认同步到 GitHub（Railway 直连 push main 即自动构建）                                                                                                                                     | 多租户 403 级数据隔离加固（13 个端点接入 get\_enterprise\_or\_403 + 18 条防回归测试）；Railway 单容器部署上线 v0.3.0（前端产物由后端同域托管，多阶段 Dockerfile 构建）                                                                                                          | 采纳。push main → CI 全绿 → Railway 自动部署，形成"提交即发布"交付链路                                          | 人工验证线上健康检查（/api/v1/health → 200/ok）、默认账号登录（admin\_t1）、生产 seed 幂等导入                                                                                                  |
|  20  |   交付  | ① 将全部工程文件/源代码/配置/脚本/依赖文档打包为项目 Workspace ZIP（含 4 步要求）；② 输出 ≤10 页 AI 生成 PPT 大纲与精准提示词；③ 输出项目全面介绍文档（6 大章节）                                                                       | 产出《税智·心判-Workspace.zip》（250 文件 / 7.6MB 压缩 / 12.0MB 原始，保留相对路径）；《AI 生成 PPT 大纲与提示词》（行为经济学/心理学重点强化，覆盖 5 大模块）；docs/project\_overview\.md（基础概述/功能模块/技术架构/规划进度/使用部署/维护贡献 6 章）                                                        | 采纳。交付物与代码库一致，全部基于真实实现撰写                                                                    | 人工完成 ZIP 四重完整性校验（testzip 无损坏 / 25 项关键路径抽查 / 实际解压 250 条目 / 中文名与 UTF-8 编码检查）；首次打包误纳 htmlcov 与 .mp4 录屏（225MB）后人工修正排除规则重新打包                                             |

### 返工与修正明细（diff 级）

> 依据实际开发记录补录，用于可核验的人工审查痕迹；未列出的交互按"一次成型、人工仅校验"处理，不虚构返工。

|  交互 |   环节  | 返工/修正点            | 具体修正（diff 级摘要）                                                                                                                                                          | 处理结果                       |
| :-: | :---: | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
|  2  |   设计  | 方案修正 1 次          | AI 初版匹配方案未覆盖"客户1 ⊂ 客户10"的子串误匹配；人工追加硬约束"禁止子串/简称模糊匹配，必须精确相等"后重新实现                                                                                                         | 修改后采纳，杜绝假阳性匹配              |
|  5  | 编码+安全 | 绑定方式修正 1 次        | AI 按 Depends() 依赖注入实现租户校验；人工发现首参名 ent\_id 与路径参数 enterprise\_id 无法绑定，改为端点函数体内直接调用 get\_enterprise\_or\_403                                                               | 绑定方式修正后落地 + 补 18 条防回归测试    |
|  6  |   编码  | 条文引用更正 1 次        | AI 初版引用《增值税暂行条例》；人工按 2026-01-01 施行的《增值税法》第十四条更正税负率维度引用，发票管理办法改引 2023 修订版第二十一条/第三十五条                                                                                     | 法律条文时效性修正，policy\_ref 全部合规 |
|  15 | 研判+编码 | 类型修复 1 次 + 否决 1 项 | ① 报告中心改造后前端 tsc 报类型错误：enterpriseApi.detail 的 mock 缺类型断言，补 `as unknown as Awaited<ReturnType<typeof enterpriseApi.detail>>`；② 否决"API 网关全局硬校验交叉绑定"过度设计，改用"构造处成对 + 成对断言测试" | tsc 恢复 0 错误；双状态口径落地        |
|  16 | 编码+测试 | 测试补桩 1 次          | fallback 用例未传参触发第 4 次 execute（兜底查最新 RiskAssessment），原 FakeDB 仅预置 3 次结果导致报错；为兜底用例单独构造含 4 个预置结果的 \_FakeDB                                                                 | 7 个一致性用例全绿                 |

***

## 四、总结

> **当前基线**（V0.3，2026-08）：累计交互 20 次（≥ 赛事要求的 10 次）；前端 tsc 0 错误、vitest 69 用例全绿（5 文件，见 TESTING.md）；后端 pytest 全绿；核心引擎覆盖率 compliance\_checker 100% / ssf\_analyzer 100% / tax\_risk\_engine 98%；生产 v0.3.0 已上线 Railway。

### 4.1 AI 生成代码比例估算：约 70%

**估算口径**（按有效代码行数与关键文件数综合估算）：

| 构成           | 说明                                                                                                                                                                                                                                                                     |
| :----------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AI 生成（约 70%） | 四流匹配引擎、7 维/5 维风险规则引擎、API 端点、schema、持久化服务、前端页面与组件（RiskMap、RiskMapCard、SSF 四象限、图表）、测试文件（backend 3 大模块 + 前端 5 个测试文件）、pytest/vitest 覆盖率配置、TESTING.md；V0.3 阶段：统一算分引擎（单条/批量）、五档风险等级映射、报告中心双状态改造、模拟器降级消除、一致性测试（7 用例）、migrate\_schema 迁移脚本、多租户 403 加固（13 端点 + 18 用例）、CI/CD 流水线 |
| 人工主导（约 30%）  | 业务规则边界裁定（精确匹配 vs 子串匹配）、**税法条文核对与更新**（增值税法 2026 施行、发票管理办法 2023 修订）、多租户安全语义（403 不泄露存在性）、Windows 环境坑（模块点分路径覆盖率、SQLite 文件锁、PowerShell 编码）、存量测试问题识别与边界界定；V0.3 阶段：算分口径治理（原始/调整后双状态成对、禁止交叉绑定）、生产根因定位（schema 漂移、chunk 404 部署窗口期）、交付物完整性把关（ZIP 校验、PPT 大纲）                       |

> 说明：AI 产出以"量大、面广"的代码骨架、完整实现与测试为主；人工贡献集中在"质量、安全、合规"三处高价值节点，虽然行数占比不高，但对交付正确性起决定性作用。

### 4.2 效能提升感受

- **交付速度**：覆盖 8 轮迭代（算法重构 → 业务改造 → 安全加固 → 测试补全 → 法规字段 → 明细展示 → 统一算分收口 → 生产保障）在极短时间内完成，若纯人工预估需数周的工作量被压缩到以"轮次"计。
- **低层劳动自动化**：重复性工程工作（序列化、CRUD、组件 mock、测试夹具、配置脚本、CI 流水线）基本由 AI 一次性完成，人工从"写代码"转向"审代码与定规则"。
- **质量门禁前置**：借助"先基线、后改动、再回归"的固定流程 + 覆盖率报告，每轮改动都有客观质量护栏，降低了回归焦虑。
- **AI 的价值上限由人设定**：最深刻的体感是——AI 能高效执行，但**方向正确性依赖人工的领域判断**。四流匹配的"精确匹配"边界、租户校验"先于缓存"的顺序、增值税法条文的时效性、V0.3 阶段"原始/调整后分"双状态口径的成对约束，这几处都是人工一句话扭转了可能走向错误的结果。AI 生成代码比例高 ≠ 人可以放手，恰恰相反，**人工的评审密度决定了 AI 产出的可用率**。
- **生产问题快速收敛**：schema 漂移（column does not exist）与 chunk 404 两类线上问题均在单轮内完成"定位根因 → 方案修复 → 回归验证"，且修复成果（幂等迁移脚本、缓存头）直接沉淀为可复用资产。
- **文档化交付**：协作日志、优化建议说明书、全面介绍文档、PPT 大纲与代码同步产出，交付物口径一致，作为赛事交付与后续交接的一手依据。
- **教训沉淀收益**：每次踩坑（Windows 文件锁、覆盖率不计入、mock 工厂清理、chunk 缓存）都固化进项目记忆，后续任务自动复用，形成"越用越顺"的复利效应。

### 4.3 反思与学习

**（1）提示词质量直接决定产出质量与返工率。**

约束式提示词（点名函数/行号/字段，如交互 3 给出 `compliance_checker.py 第 387 行`）一次采纳率高、几乎零返工；泛化提示词则容易产出"可用但不合口径"的结果（如交互 2 未约束"精确匹配"，AI 默认给出子串匹配方案被人工修正）。**复盘结论**：涉及业务规则的提示词，必须先将边界与"必须/禁止"讲清，再让 AI 实现——提示词即评审前置。

**（2）人机分工的正确姿势是"AI 管面、人工管点"。**

AI 的价值在"量与面"（骨架、完整实现、测试批量补齐），人工的价值在"质与向"（口径、安全、法律时效、架构红线）。V0.3 双状态口径是最典型例证：AI 能按指令完成全部改造，但"原始/调整后分必须成对、禁止交叉绑定"这条治理红线由人工裁定（交互 15）；审计建议中"全局硬校验"被人工判为过度设计而否决——**采纳率的健康度不只看采纳多少，更看否决与修正是否得当**。

**（3）验证意识是真实性的底线。**

chunk 404 若不经 curl 验证生产 index.html 的实际 hash，就会误判为构建缺陷（交互 18）；覆盖率不计入若不做路径口径排查，就会被"达标"假象误导（交互 7）。**AI 输出必须以真实证据与既有基线（TESTING.md）核对后再采纳**——这也构成本日志"不造假"的制度保障。

**（4）评审制度化优于事后救火。**

从交互 9 起固化"先基线、后改动、再回归"门禁后，所有 AI 改动都有客观护栏；17/20 完全采纳的背后是"采纳前必审、审完必测"。**协作模式演进**：早期（交互 1-6）"委托-校对"（AI 全权实现、人工校对边界）→ 中期（交互 7-12）"门禁-回归"（建立基线、量化验收）→ 后期（交互 13-20）"评审-收口"（人工先定口径规则、AI 按规则执行并以测试固化）——人工前置规则的比例逐步上升，这是项目从 v0.1 走到 v0.3.0 的核心方法论。

### 4.4 教训沉淀清单

|  #  | 教训条目                                         | 触发场景                   | 沉淀去向（可复用资产）                            |
| :-: | -------------------------------------------- | ---------------------- | -------------------------------------- |
|  1  | pytest --cov 必须用模块点分路径（app.core.xxx），相对路径不计入 | 覆盖率统计为 0 的误判           | 项目记忆 + pytest.ini                      |
|  2  | Windows 下 SQLite 临时文件句柄锁（WinError 32）        | pytest teardown 偶发失败   | tests/test\_api/conftest.py 重试机制       |
|  3  | vi.restoreAllMocks 会清空 vi.mock 工厂内实现         | 前端 mock 神秘失效           | mock 规范：依赖实现的 mock 必须 beforeEach 重设    |
|  4  | matchMedia polyfill 必须用普通函数而非 vi.fn()        | 组件测试渲染报错               | 前端测试规范固化                               |
|  5  | Base.metadata.create\_all 只建新表、不补新列          | 生产 schema 漂移全接口 500    | scripts/migrate\_schema.py 幂等同步 + 部署手册 |
|  6  | index.html 需加 Cache-Control: no-cache        | 部署后旧页面引用已删 chunk 404   | backend/app/main.py 缓存头 + 硬刷新指引        |
|  7  | 测试桩必须预置全链路 execute 次数                        | fallback 用例第 4 次查询缺桩报错 | 测试用例编写规范                               |
|  8  | PowerShell 提交消息乱码仅显示层问题                      | git 提交显示异常             | 以 .git/COMMIT\_EDITMSG 验证存储编码为 UTF-8   |

### 4.5 提示词约束模板

> 从 20 次交互中提炼的统一提示词格式，用于约束 AI 产出边界、降低口径漂移；后续新交互一律按此模板书写。

```
# 统一提示词模板
【目标】一次只交代一件事（如：统一全项目算分收口）
【范围】点名函数/文件/接口（如：compute_compliance_adjusted_risk；risk_scan.py 第 387 行）
【必须】不可妥协的硬要求（如：必须精确相等匹配；必须返回 original/adjusted 成对字段；必须用 decimal）
【禁止】明确否决的行为（如：禁止浮点运算；禁止子串模糊匹配；禁止另起独立算分函数；禁止改历史快照）
【验收】可核验标准（如：补 ≥7 个测试；前端 tsc 0 错误；后端 pytest 全绿）
```

**模板化示例**（交互 13 的原始提示词按模板归一）：

| 槽位 | 内容                                                                                                               |
| -- | ---------------------------------------------------------------------------------------------------------------- |
| 目标 | 统一全项目风险算分收口，前端"原始分/整改后分"双栏展示                                                                                     |
| 范围 | backend：compute\_compliance\_adjusted\_risk / compute\_compliance\_adjusted\_risks；frontend：风险地图、数据驾驶舱、整改追踪、报告中心 |
| 必须 | 返回 original\_score/original\_level 与 adjusted\_score/adjusted\_level 成对；兜底取数；整改完成率真实计算                           |
| 禁止 | 禁止任何页面或接口另起算分逻辑；禁止覆盖历史快照                                                                                         |
| 验收 | 批量=单条交叉一致性测试；tsc 0 错误                                                                                            |

